
@model IEnumerable<string>

@foreach (string menuItem in Model)
{
    <a class="btn btn-block @(menuItem == ViewBag.SelectedMenuItem ? "btn-primary" : "btn-secondary")"
       asp-action="Index"
       asp-controller="@menuItem">
        @menuItem
    </a>
}


